#include <iostream>
#include <cstdlib>
#include <vector>			// to allow using the STL vector class
#include <string>		// to allow using the C++ string class

using namespace std;

int main()
{
//Create a vector
vector<str> names;

iter=grades.begin();
//Add 10 elements
names.push_front(A);
names.push_back(B);
names.push_back(C);
names.push_back(D);
names.push_back(E);
names.push_back(F);
names.push_back(G);
names.push_back(H);
names.push_back(I);
names.push_back(J);
//Print out
cout << names

}
